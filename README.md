# 电气工程计算自动化平台

面向具备一定电气基础的用户、运行在 Windows 本机的低压回路计算工具。

当前发布目标为 `v0.1.0`：交付从变压器低压出口、低压柜、分配电箱到用电设备末端的完整单回路计算。项目管理、批量导入、复杂多电源网络和成果导出仍属于试验性能力或后续版本，不作为本阶段产品承诺。详细边界见 [v0.1.0 阶段发布验收](docs/V0.1.0阶段发布验收.md)。

## 两种工作流

- **快速计算**：用户只填写功率或电流、相制、长度等少量已知条件，系统自动推导和查表，输出不绑定品牌的电缆及断路器通用规格候选。
- **图纸核验**：图纸作为既有设计条件，系统沿“变压器低压出口 → 低压柜 → 分配电箱 → 用电设备”逐段复算，分别判断原电缆和原断路器。替代候选单独显示，不能代替原设计合规结论。

## 0.4kV 回路计算向导

打开 `http://127.0.0.1:8765/quick`，无需新建项目或导入 Excel。页面支持“单台设备、同类负荷汇总、配电箱或干线”三种回路角色，可按 kW、kVA 或已知电流计算。单台设备会按负荷资料自动调整适用相制；同类单相设备可按三相均衡汇总，但必须确认相负荷分配。

kW 可明确为已经确定的计算功率，或安装功率。安装功率模式必须由用户填写需要系数，系统不会猜测默认值。kW 模式可填写铭牌/厂家功率因数；未填写时按所选设备的功率因数范围下限保守初选。“其他/不确定”负荷必须填写功率因数才能由 kW 计算电流。

快速页依据项目内 19DX101-1 的已核实表格给出不绑定品牌的 MCB 设计参数候选，包括额定电流、额定电压、电流规格等级及分断能力档位。D型MCB及MCCB的具体产品脱扣/整定参数归入后续电动机模块。回路位于变压器0.4kV低压出口时，可按表15.7选择变压器容量和铭牌uk%速查短路电流；只知道容量时显示表列范围并按上限暂选分断能力。RCD 按用途/场所给出剩余动作电流候选，并按用户确认的剩余电流波形给出类型候选；具体产品动作时间仍待核验。

载流量表或必要校核未批准时，页面显示“基础初选”和未完成项目，正式状态保持“无法判断”。BV 穿管会根据普通回路相制自动匹配2根或3根载流导线的表列工况；YJV 目前以已核实的三芯明敷导管/空气中数据作为截面初选依据，但不会据此认定实际电缆芯数。高级条件可选择实际环境空气温度或地下温度，系统按19DX101-1表6.22或表6.24自动修正载流量并重新选择截面。芯数、N/PE 和断路器极数仍需确认。本页面不会写入项目、回路或计算历史。

高级条件中只需填写线路长度即可继续计算电压降。系统根据最小满足截面，从已核实的19DX101-1表格自动查取 R/X；普通低压线路自动采用5%，供给有照明负荷的低压线路按手册表6.2-6的3%～5%范围取3%作保守暂算。该限值规则目前已核实、未批准。

## 完整回路核验

打开 `http://127.0.0.1:8765/complete-circuit`。公开版本内置匿名验收夹具，用于证明同一入口能够完成：

- 上级系统短路容量与变压器表列R/X解析；
- 各线路段负荷流、载流量和累计电压降；
- 各节点最大三相短路、最小相—N短路和TN相—PE故障电流；
- 原图断路器的 `Ib≤In≤Iz`、分断能力和自动切断条件；
- PE最小截面、相导体/PE热稳定允许约束；
- 上下级选择性/后备保护的证据完整性检查。

没有准确产品曲线、I²t或厂家配合表时，对应项目显示“无法判断”，不会猜测通过。

## 已实现的计算基础

- 负荷计算与电缆/保护器件配合暂算
- 电压降计算与按回路类型自动采用的限值暂算
- 三相对称短路电流简化计算与分断能力暂算
- 多段完整放射式回路的逐节点短路与接地故障计算
- 图纸原部件复核与独立替代候选
- 电缆和保护器件组合求解
- 计算依据的待核实、已核实、已批准状态管理

仓库中可能保留项目、Excel 和 PDF 的试验性实现，但这些功能不纳入 `v0.1.0` 验收；真实工程图纸只用于本地回归检验，不随公开版本发布。

> **重要：** 规范、手册、产品样本和真实图纸不随公开仓库发布。平台内置数据保留来源定位和状态；关联依据未批准或必要校核未完成时，正式状态保持“无法判断”。

## 启动

首次使用，在 PowerShell 中执行：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe run.py
```

平台默认在 `http://127.0.0.1:8765` 启动并自动打开浏览器。无需自动打开时：

```powershell
.\.venv\Scripts\python.exe run.py --no-browser
```

本机项目库默认位于 `data/electrical_calc.db`。如需使用其他数据库，可在启动前设置 `ELECTRICAL_CALC_DB`。

## 建议工作流

1. 只知道负荷条件并准备采购时，使用“快速计算”。
2. 已有图纸并需要判断原设计时，使用“图纸核验”。
3. 先查看原图设计复核，再查看独立替代候选；两栏结论不得互换。
4. 对“无法判断”项目补充铭牌、产品曲线或厂家配合表后再复核。

## 数据约束

- 内部固定使用 V、kW、A、m、Ω、Ω/km、kA 和百分数。
- 缺失、不适用和数值 0 严格区分。
- 未给出的参数不会被默认值替代。
- 三个模块共享同一回路输入；任何更新都会递增回路修订号。
- 每次计算保存输入、依据、过程、结果和警告的完整快照。
- 只有关联规则全部为“已批准”时，平台才会把暂算通过/不通过提升为正式状态。

## 测试

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

测试覆盖快速计算、完整回路、缺失输入、适用范围、逐节点故障电流、图纸原器件核验、替代候选隔离以及网页主流程。

## 目录

```text
src/electrical_calc/
├── engine.py          # 与界面、数据库解耦的计算核心
├── complete_circuit_engine.py # 完整回路逐段/逐节点计算
├── radial_circuit_service.py  # 放射式回路统一编排
├── drawing_audit.py   # 图纸原部件复核
├── database.py        # SQLite 模型与不可覆盖计算记录
├── spreadsheets.py    # Excel 导入、模板及成果导出
├── reports.py         # PDF 计算书
├── web.py             # FastAPI 页面与接口
├── templates/         # 服务端页面模板
└── static/            # 页面样式

data/templates/       # 已校验的 Excel 导入模板
data/references/       # 用户提供并核实的规范资料索引
tests/                 # 自动化测试
```

现有立项分析和早期路线资料保留在 `docs/` 中。
